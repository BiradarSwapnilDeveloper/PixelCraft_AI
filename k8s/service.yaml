apiVersion: v1
kind: Service
metadata:
  name: pixelcraft-ai-service
  labels:
    app: pixelcraft-ai
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: pixelcraft-ai
